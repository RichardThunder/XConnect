#!/bin/sh
# 登录后自动启动：后台运行 xconnect -daemon -sync，再启动托盘
# 由 /etc/xdg/autostart/xconnect.desktop 调用

PATH="/usr/bin:$PATH"
export PATH

# 先启动同步服务（后台，带 -daemon -sync）
xconnect -daemon -sync &
# 再启动托盘（前台，托盘进程常驻）
exec xconnect-tray
